package com.shuttershot.model;

// The eight shelves of the Accessories Marketplace. Display names live in the
// frontend; the backend only needs the stable key.
public enum ProductCategory {
    ESSENTIALS,
    LIGHTING,
    LENSES,
    SUPPORT,
    STORAGE,
    AUDIO_VIDEO,
    SOFTWARE,
    EVERYDAY
}
